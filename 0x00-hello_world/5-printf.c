#include <stdio.h>
/**
 * main - prints "with proper grammar, but the outcome is a piece of art" followed by newline
 * Return: Always ).
 *
 */
int main(voi)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0)'
}
